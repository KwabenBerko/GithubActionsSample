name: Android CI

on: [push]

jobs:
#  build:
#    name: Run Build
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: set up JDK 1.8
#        uses: actions/setup-java@v1
#        with:
#          java-version: 1.8
#      - name: Build
#        run: ./gradlew build
#
#  test:
#    name: Run Unit Tests
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: set up JDK 1.8
#        uses: actions/setup-java@v1
#        with:
#          java-version: 1.8
#      - name: Unit Tests
#        run: ./gradlew test
#
#  androidTest:
#    name: Run Android Instrumentation Tests
#    runs-on: macOS-10.15
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: set up JDK 1.8
#        uses: actions/setup-java@v1
#        with:
#          java-version: 1.8
#      - name: Instrumentation Tests
#        uses: reactivecircus/android-emulator-runner@v2
#        with:
#          api-level: 21
#          script: ./gradlew connectedAndroidTest

  codeCoverage:
    name: Run Code Coverage Tests
    runs-on: macOS-10.15

    steps:
      - uses: actions/checkout@v2
      - name: set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Code Coverage Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 21
          script: ./gradlew jacocoTestCoverageVerification

      - name: Publish Code Coverage To Code Climate
        run: |
          ./gradlew jacocoFix
          export GIT_BRANCH="${GITHUB_REF/refs\/heads\//}"
          curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-darwin-amd64  > ./cc-test-reporter
          chmod +x ./cc-test-reporter
          ./cc-test-reporter before-build
          ./cc-test-reporter format-coverage -t jacoco
          ./cc-test-reporter upload-coverage -d
        env:
          CC_TEST_REPORTER_ID: 0762e2e0f516de5ebe1df0f0c628dcf26ea02008e766aed8ba7d1001aa442410

#        uses: paambaati/codeclimate-action@v2.4.0
#        env:
#          CC_TEST_REPORTER_ID: 0762e2e0f516de5ebe1df0f0c628dcf26ea02008e766aed8ba7d1001aa442410
#        with:
#          coverageCommand: JACOCO_SOURCE_PATH=src/main/java ./cc-test-reporter \format-coverage target/site/jacoco/jacoco.xml\ --input-type jacoco ./cc-test-reporter upload-coverage
#          debug: true
#        run: |
#          export GIT_BRANCH="${GITHUB_REF/refs\/heads\//}"
#          curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
#          chmod +x ./cc-test-reporter
#          JACOCO_SOURCE_PATH=src/main/java ./cc-test-reporter \format-coverage target/site/jacoco/jacoco.xml\ --input-type jacoco ./cc-test-reporter upload-coverage

